# Image fixtures

These synthetic 8×6 images contain blue and yellow rectangles and were created specifically for this repository. They may be used under the repository's MIT license.

The same source image is encoded as every supported input format so browser tests can verify the complete decoder/encoder matrix. `input.heif` intentionally contains the same HEVC image as `input.heic`; the MIME type and extension exercise the two public input cases.
